generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
}

model Alunos {
    id        Int      @id @default(autoincrement())
    nome      String
    turma     String
    materia  String
    foto      String?

    @@map("aluno")
}
